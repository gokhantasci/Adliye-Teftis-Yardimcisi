# Environment Configuration Template
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control

# Email Configuration (Required for send-mail.php)
# Gmail account credentials for sending emails
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-app-specific-password

# Application Settings (Optional)
# Uncomment and configure as needed

# PHP Configuration
# PHP_DISPLAY_ERRORS=0
# PHP_ERROR_REPORTING=E_ALL

# Data Directory (default: ./data)
# DATA_DIR=/path/to/data

# Timezone
# TZ=Europe/Istanbul

# Development/Production Mode
# APP_ENV=production
# APP_DEBUG=false

# Notes:
# - GMAIL_APP_PASSWORD should be generated from Google Account settings
# - Do not use your regular Gmail password
# - Emails are restricted to @adalet.gov.tr domain
# - Ensure proper file permissions for data directory
